# AgentFlow Entry

Use `/workflow <task>` or `/agentflow <task>` in OpenCode.

If the plugin is unavailable, run:

`npm run workflow:run-profile -- --profile agent-workforce-basic --task "<task>"`

If `<auto-slash-command>` appears, an old markdown `/workflow` command is still installed.
